# Exemplars (范文)

把多篇 IELTS Task 2 范文放到此目录（建议 `.md` 或 `.txt`）。

格式约定（用于解析结构）：
- 段落用空行分隔（即 `\n\n`）。
- 每段建议是连续文本；首句会被当作该段的“主题句/主题提示”。

应用会在运行时自动读取此目录下的文件，并把“范文段落结构摘要”注入 AI prompt，用来引导：
- 当前段落应该讲什么（同段保持一个 controlling idea）
- 何时应该另起一段

注意：为控制 prompt 长度，程序只会取每篇范文的段落数 + 每段首句（截断后）作为摘要，不会把整篇范文塞给模型。
